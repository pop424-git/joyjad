# จอยจัด 🏸

เว็บแอปหน้าเดียวสำหรับจัดคิวก๊วนแบดมินตัน จองได้ 1-4 สนามต่อวัน (เลือกเลขสนามจริงได้จากสนามที่มีทั้งหมด 10 สนาม) เล่นคู่ ระบบเข้า 2 ออก 2
ธีมชมพู-ขาว · ออกแบบให้มือถือเป็นหลัก · iPad เครื่องเดียวเป็นคนจัด คนอื่นเปิดลิงก์ดูสดได้ทุกเครื่อง

**ใช้งานจริง:** https://joyjad.vercel.app
**พาทัวร์ทีละหน้าจอ** (หน้าเดียว ส่งให้คนจัดก๊วนดู): https://joyjad.vercel.app/guide — สร้างจาก [.claude/presentation/](.claude/presentation/)

เปิดลิงก์เฉยๆ = **จอดูสด** (ดูอย่างเดียว) · จะจัดคิวหรือตั้งนัดต้องเข้า **โหมดแอดมิน** ก่อน — แตะชื่อ *จอยจัด* 5 ครั้ง ([รายละเอียด](#โหมดแอดมิน-กับ-จอดูสด))

หน้าแรกแสดง **นัดครั้งถัดไป** (ถ้าแอดมินตั้งไว้) หรือเวลาประจำของก๊วน พร้อมกติกาก๊วน
เวลาประจำ (Grand Valley · ศุกร์ 19:00-21:00 · อาทิตย์ 18:00-20:00) แก้ได้ที่ฟังก์ชัน `defaultHeroHTML()` ใน [index.html](index.html)

## นัดครั้งถัดไป (แอดมินตั้งเอง ทุกคนเห็น)

รู้ล่วงหน้าว่าได้สนามไหน กี่โมง ใครมาบ้าง — ตั้งไว้บนหน้าแรกได้เลย ไม่ต้องแก้โค้ด

- **แอดมิน** กดปุ่ม **ตั้งนัดวันนี้** (ใต้เวลาประจำ) หรือ **แก้ไขนัด** (มุมขวาบน เมื่อมีนัดแล้ว) → กรอกสนาม, วันที่, เวลาเริ่ม-เลิก, เลขคอร์ท (ถึง 4), แตะเลือกคนที่มาจากรายชื่อส่วนกลาง (เพิ่มชื่อใหม่ได้ตรงนั้น)
- **คนทั่วไป** เห็นการ์ดอย่างเดียว ปุ่มแก้ไม่ขึ้น
- ชื่อก๊วนบนหน้าแรกขยายให้เองตามจอ — ใหญ่สุดเท่าที่ยังอยู่บรรทัดเดียว
- บันทึกแล้ว **เติมให้หน้าตั้งค่าอัตโนมัติ** — จำนวนสนามกับเลขคอร์ทตามที่ประกาศ และขั้นที่ 2 เลือกคนในนัดไว้ให้ (แตะแก้ทีหลังได้ตามปกติ)
- การ์ด **หายเองหลังเลยเวลาเลิก 3 ชั่วโมง** (เผื่อตีเกินเวลา/เคลียร์เงิน) หรือกด **ล้างนัด** เอง
- เก็บบนเซิร์ฟเวอร์คนละที่กับสถานะก๊วน — **จบก๊วนแล้วการ์ดนัดยังอยู่** จนกว่าจะเลยเวลา
- ตั้งนัดต้องต่อเน็ตได้ (ใช้ `/api/state` เส้นเดียวกับจอดูสด) ถ้ายังไม่ได้ตั้ง Upstash การ์ดนี้จะใช้ไม่ได้ หน้าแรกจะโชว์เวลาประจำแทน

## เริ่มใช้งาน (3 ขั้น)

1. **เลือกจำนวนสนาม** — 1-4 สนาม (1 สนามเล่นได้ 4-8 คน) แล้วแตะเลือกเลขสนามจริงที่จะใช้ (มีให้เลือก 10 สนาม เลือกได้ไม่ติดกันก็ได้ — เลือกครบเมื่อไหร่ปุ่มถัดไปถึงกดได้)
2. **เลือกผู้เล่น** — แตะชื่อจากรายชื่อที่จำไว้ หรือพิมพ์เพิ่มคนใหม่ · ตัวเลขบนชื่อ = ลำดับที่เลือก (ใช้เป็นลำดับคิว)
3. **เลือกคนลงสนาม** — แตะชื่อจากรายการล่างเพื่อใส่ลงสนามที่กำลังไฮไลต์ ครบ 4 คนแล้วเลื่อนไปสนามถัดไปเอง · แตะชื่อในสนามเพื่อเอาออก · หรือกด **จัดให้อัตโนมัติ** → **เริ่มจัดก๊วน**

## กติกาหมุนเวียน

- **เกมแรกของสนาม** — กด **จบเกม** → เลือก 2 คนที่จะออก
- **เกมถัดๆ ไป** — คนที่เล่นครบ 2 เกมติดถูกเลือกออกให้อัตโนมัติ กดยืนยันได้เลย (แก้ก่อนได้)
- คนที่ออกไปต่อท้ายคิวโดยลำดับติดกัน แต่ไม่ผูกว่าต้องลงพร้อมกันอีก
- หน้ายืนยันโชว์ **ใครออก / ใครลงแทน** เสมอ แตะเปลี่ยนได้ก่อนกดยืนยัน
- ตำแหน่งยืนในสนามไม่ล็อก ผู้เล่นตกลงกันเอง
- ถ้าไม่มีคนรอคิวเลย กดจบเกมจะนับเกมให้ทุกคนแล้วเล่นต่อได้เลย
- กดยืนยันแล้ว **ขึ้นป้ายเต็มจอ + อ่านชื่อออกเสียง** — พื้นชมพูเข้ม ชื่อคนที่ต้องลงตัวขาวใหญ่เต็มพื้นที่ (ปรับขนาดให้เองตามความยาวชื่อและขนาดจอ · มือถือเรียงชื่อละบรรทัด)
  - หายเองใน **7 วินาที** บนเครื่องที่กด (แอดมิน) · **5 วินาที** บนเครื่องอื่น · แตะที่ป้ายเพื่อปิดก่อนได้
  - เสียงพูดมีเฉพาะเครื่องแอดมินที่กด เครื่องอื่นเห็นป้ายอย่างเดียว
  - **ขึ้นทุกเครื่องที่เปิดอยู่** ไม่ใช่แค่ iPad ที่กด (เครื่องอื่นช้ากว่าได้ถึงราว 5 วินาทีตามรอบซิงก์)

## คิวรอ

- เรียงลำดับ 1, 2, 3... — **กรอบชมพู = 2 คนถัดไปที่จะได้ลง**
- ปุ่ม ▲▼ เลื่อนคิวขึ้น/ลง (เช่นอยากดันคิวที่ 3 ขึ้นมาก่อน)
- "N เกม" = จำนวนเกมที่คนนั้นเล่นไปแล้ว

## ระหว่างเกม

- **แตะชื่อคนในสนาม** = เปลี่ยนตัวกลางคัน คนที่ออกจะไปรับคิวของคนที่ลง (ไม่นับเป็นจบเกม)
- **ช่องว่าง** = แตะเพื่อดึงคนจากคิวลง
- **รายชื่อ** = เพิ่มคนมาสาย / ปิดสวิตช์คนที่กลับบ้าน (ออกจากสนามและคิวทันที แต่ยังนับในการหารเงิน)

## จบก๊วน

กด **สิ้นสุดก๊วน** บนหัวจอ:

1. **สรุปเกม** — ใครเล่นไปกี่เกม เรียงมากไปน้อย (กลับไปเล่นต่อได้)
2. **คิดเงิน** — กรอกค่าสนามรวม + จำนวนลูกแบดที่ใช้ + ราคาต่อหลอด (12 ลูก)
   - ค่าลูก = ลูกที่ใช้ ÷ 12 × ราคาหลอด
   - **หารเท่ากันทุกคนที่มาวันนี้ ปัดขึ้นเป็นบาท** พร้อมบอกยอดที่เก็บได้และเศษที่เกิน
   - ค่าสนามกับราคาหลอดถูกจำไว้ให้ครั้งหน้า
3. กด **เริ่มจัดก๊วนใหม่** เพื่อเริ่มรอบใหม่

### QR จ่ายเงิน

- **แอดมินอัปครั้งเดียว** ที่หน้าคิดเงิน → **เลือกรูป QR** (พร้อมเพย์/ธนาคาร) + ใส่ชื่อบัญชี
  - รูปถูกย่อให้เอง (ด้านยาว 700px, JPEG) — ไฟล์ 180KB เหลือราว 70KB
  - เก็บบน Upstash คนละ key กับก๊วน **อยู่ข้ามก๊วน** ไม่ต้องอัปใหม่ทุกครั้ง (อยู่ได้ 180 วัน ต่ออายุเมื่ออัปใหม่)
  - **ไม่ได้อยู่ในโค้ด** — repo เป็น public รูปกับชื่อบัญชีเลยไม่ถูก commit
- อัปแล้วปุ่ม **💸 จ่ายเงิน** โผล่บนหัวจอ **ทุกเครื่องที่เปิดอยู่** (คนดูสดกดได้ด้วย) → เต็มจอ: **ยอดต่อคนตัวใหญ่** + QR + ชื่อบัญชี
- QR พร้อมเพย์แบบนี้ไม่ได้ล็อกยอด คนสแกนกรอกเงินเอง — เลยโชว์ยอดต่อคนไว้ข้างๆ
- รูปถูกแคชในเครื่องที่เคยเปิด **เน็ตหลุดก็ยังเปิดดูได้** (แต่ยอดต่อคนจะขึ้น — ถ้าดึง state ไม่ได้)
- แก้ชื่อบัญชีทีหลังได้ (พิมพ์แล้วกดออกจากช่อง) · กด **ลบ QR** แล้วปุ่มหายทุกเครื่อง

## รายชื่อส่วนกลาง (ทุกเครื่องเห็นตรงกัน)

รายชื่อผู้เล่นเก็บบนเซิร์ฟเวอร์ ผ่าน `/api/roster` (Vercel KV / Upstash Redis)

- ใครเปิดลิงก์ก็ **เพิ่ม/ลบชื่อได้** ไม่มีล็อกอิน
- ชื่ออยู่ถาวรจนกว่าจะกดปุ่ม **ล้างรายชื่อ** (ถามยืนยัน 2 ครั้ง เพราะกระทบทุกเครื่อง)
- แถบสถานะบนหน้าเลือกผู้เล่น:
  - 🟢 **รายชื่อส่วนกลาง N คน** = ซิงก์ปกติ
  - 🔴 **ต่อส่วนกลางไม่ได้** = ใช้รายชื่อที่แคชไว้ในเครื่องต่อได้ ชื่อที่เพิ่มตอนเน็ตหลุดจะถูกส่งขึ้นให้เองเมื่อกลับมาต่อได้
- กด **รีเฟรช** เพื่อดึงชื่อล่าสุดที่เครื่องอื่นเพิ่งเพิ่ม

### ตั้งค่า Vercel KV / Upstash (ทำครั้งเดียว)

1. เปิดโปรเจกต์ใน Vercel → แท็บ **Storage** → เพิ่ม **Upstash Redis** (มี free tier)
2. Vercel จะใส่ environment variables ให้เอง — ตัวที่แอปใช้คือ
   `KV_REST_API_URL` + `KV_REST_API_TOKEN` (หรือ `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN`)
3. **Redeploy** หนึ่งครั้งให้ตัวแปรมีผล

ยังไม่ได้ตั้งค่า → `/api/roster` ตอบ 503 แอปจะใช้รายชื่อในเครื่องแทน ใช้งานได้ปกติแค่ไม่ซิงก์

## โหมดแอดมิน กับ จอดูสด

**เปิดลิงก์เฉยๆ = จอดูสด** อ่านอย่างเดียว — เห็นสนาม คิว การ์ดนัด และป้ายเรียกลงสนาม แต่กดอะไรไม่ได้

**เข้าโหมดแอดมิน:** แตะที่ชื่อ **จอยจัด** 5 ครั้งใน 2.5 วินาที (แตะที่หน้าแรก หรือหัวจอตอนเล่นก็ได้) → หน้าจอโหลดใหม่เข้าโหมดแอดมิน · แตะ 5 ครั้งอีกทีเพื่อออก
เครื่องนั้นจำโหมดไว้ใน localStorage — ตั้งครั้งเดียวที่ iPad ที่ใช้จัดคิว ที่เหลือเปิดลิงก์ดูอย่างเดียว

- แอดมินกดอะไร **ทุกเครื่องเห็นตาม** ผ่าน `/api/state` — จอดูสดดึงทุก 5 วินาที แอดมินเช็คอินทุก 8 วินาที
- ป้าย **👀 N คนกำลังดู** (ฝั่งแอดมิน) / **N คนออนไลน์** (ฝั่งคนดู) = จำนวนเครื่องที่เปิดหน้านี้อยู่
- ไฟบนหัวจอ: 🟢 **บันทึก HH:MM** (แอดมิน) · 🟢 **ดูสด HH:MM** (คนดู) · 🔴 = ต่อไม่ได้ หรือเบราว์เซอร์ปิดการเก็บข้อมูล (มักเป็น Safari Private Browsing)
- มีแอดมินหลายเครื่องได้ — เครื่องไหนจบก๊วน/เริ่มใหม่ เครื่องอื่นตามให้เอง
- ก๊วนบนเซิร์ฟเวอร์หมดอายุเองใน 5 ชั่วโมง (เผื่อลืมกดสิ้นสุด)

### เปิดใหม่แล้วก๊วนหายไหม

สถานะก๊วนเก็บใน localStorage ของเครื่องแอดมินด้วย

- เปิดแอปครั้งถัดไป → ขึ้นหน้าเลือกสนามใหม่เสมอ
- **ยกเว้น** มีก๊วนค้างไม่เกิน 5 ชั่วโมง → กู้กลับให้อัตโนมัติ (เผื่อรีเฟรช/เครื่องดับกลางก๊วน)
- ถ้าเครื่องแอดมินหาย แต่มีอีกเครื่องเป็นแอดมินอยู่ ก๊วนบนเซิร์ฟเวอร์ยังอยู่ ดึงกลับมาได้

## อัปขึ้น Vercel

โปรเจกต์นี้ต่อกับ GitHub ([pop424-git/joyjad](https://github.com/pop424-git/joyjad)) แล้ว — **push ขึ้น `main` แล้ว Vercel deploy ให้เองภายในไม่กี่นาที**

```bash
git push origin main
```

### หรือแบบลาก-วาง (ไม่ต้องใช้ Git)

1. เปิด https://vercel.com/new
2. เลื่อนลงหา **"or drag and drop"**
3. ลาก **โฟลเดอร์ `badminton-app`** ทั้งโฟลเดอร์เข้าไปวาง (อย่าลากแค่ไฟล์ `index.html` — โฟลเดอร์ `api/` ต้องไปด้วย)
4. กด **Deploy** รอประมาณ 20-30 วินาที
5. ตั้งค่า Storage ตามหัวข้อด้านบน แล้ว redeploy

## ทดสอบบนเครื่องตัวเอง

```bash
node .claude/serve.js
```

เปิด http://localhost:5173 (ต้องผ่าน server ไม่ใช่ดับเบิลคลิกไฟล์ ไม่งั้น localStorage ไม่ทำงาน)
dev server จำลอง `/api/roster` กับ `/api/state` (รวมนัดกับ QR จ่ายเงิน) ไว้ในหน่วยความจำให้ด้วย — ทดสอบซิงก์ได้โดยไม่ต้องต่อ Upstash
รูปที่ลากมาวางในโฟลเดอร์ (`*.jpg`, `*.png`) ถูก .gitignore ไว้ — ใช้ทดสอบอัปได้ ไม่หลุดขึ้น repo

ปุ่มลับสำหรับทดสอบ: `/api/_mock?clear=1` ล้างของจำลองทั้งหมด · `?seed=ชื่อ,ชื่อ` ใส่รายชื่อ · `?down=1` จำลองเน็ตหลุด · `?expireplan=1` ดันนัดให้หมดอายุทันที

## ไฟล์ในโฟลเดอร์

- `index.html` — ตัวแอปทั้งหมด (HTML + CSS + JS ไฟล์เดียว ไม่มี dependency)
- `api/roster.js` — API รายชื่อส่วนกลาง (Vercel serverless function, ไม่ใช้ npm package)
- `api/state.js` — สถานะก๊วนสด (จอดูสด) + นัดครั้งถัดไป + QR จ่ายเงิน เก็บคนละ key กัน
- `vercel.json` — ตั้งค่า cache
- `index-old.html` — เวอร์ชันก่อนปรับ (2 สนามตายตัว + เมนูแชร์) ลบทิ้งได้
- `.claude/serve.js`, `.claude/launch.json` — dev server สำหรับทดสอบ ไม่เกี่ยวกับตัวแอปตอน deploy
